// PlanetGen.cpp
// Module implementation file. Every UE module needs exactly one of these -- it's what
// actually registers the module with the engine's module manager via IMPLEMENT_MODULE.
// Without this, UBT may auto-generate a minimal stub, which can behave inconsistently
// with CoreUObject's reflection/registration startup across engine versions.
#include "Modules/ModuleManager.h"

IMPLEMENT_MODULE(FDefaultModuleImpl, PlanetGen)
